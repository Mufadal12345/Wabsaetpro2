import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.museumofthought.app',
  appName: 'Museum of Thought',
  webDir: 'dist'
};

export default config;
